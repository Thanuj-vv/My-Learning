class Solution:
    def reverseArray(self, arr):
        # code here
        l=0
        e=len(arr)-1
        for l in range(len(arr)//2):
            
            arr[e],arr[l]=arr[l],arr[e]
            
            l+=1
            e-=1
        return arr
